spring:
  application:
    name: blog
  cloud:
    config:
      uri: http://localhost:8888
      username: config
      password: config
      profile: dev
      label: master
      enabled: true

encrypt:
  key: configKey